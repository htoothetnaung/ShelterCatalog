{
  "name": "petfinder-login-app",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "start": "node server.js",
    "build": "ng build --configuration production && copy server.js dist\\",
    "watch": "ng build --watch --configuration development",
    "test": "ng test"
  },
  "dependencies": {
    "express": "^4.18.2",
    "path": "^0.12.7",
    "fs": "0.0.1-security"
  }
}