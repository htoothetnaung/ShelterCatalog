{
  "name": "petfinder-login-app",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "npx ng build --configuration production",
    "watch": "ng build --watch --configuration development",
    "test": "ng test"
  },
  "devDependencies": {
    "@angular/cli": "^19.1.7"
    
  }
}
